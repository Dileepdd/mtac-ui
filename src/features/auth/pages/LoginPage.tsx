// Phase 3 — full implementation coming
export default function LoginPage() {
  return (
    <div style={{ display: "flex", alignItems: "center", justifyContent: "center", height: "100vh" }}>
      <p className="mono" style={{ color: "var(--text-3)" }}>Login — Phase 3</p>
    </div>
  );
}
