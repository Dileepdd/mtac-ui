# Base URL for the MTAC backend API
VITE_API_URL=http://localhost:3006/api

# Google OAuth callback — must match GOOGLE_CALLBACK_URL in the backend .env
# The backend redirects to: {VITE_APP_URL}/auth/callback after Google login
VITE_APP_URL=http://localhost:5173
